= provide(:title, "Anziel")
#jobs
	- @jobs.in_groups_of(4, false) do |jobgroup|
		.jobgroup.col-lg-3.col-md-4.col-sm-6
			- jobgroup.each do |job|
				.job
					%h4= link_to job.title, job
					%p= job.parish.name
					%p= job.company